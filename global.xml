<!--
   Ant build file for deegree.
   Global properties. Edit this file ONLY to apply new properties
   $Workfile: global.xml $, $Author: rbezema $
   $Revision: 10888 $, $Date: 2008-04-02 09:28:33 +0200 (Wed, 02 Apr 2008) $
 -->

 <!--
   set global properties for this build
   it's recommended to keep these definitions
 -->
  <property name="app.name"       value="deegree2" />
  <property name="src"            value="${basedir}/src" />
  <property name="build"          value="${basedir}/classes" />
  <property name="dist"           value="${basedir}/dist" />
  <property name="dist.lib"       value="${dist}/lib" />
  <property name="lib.jar"        value="${app.name}.jar" />
  <property name="lib"            value="lib" />
  <property name="optional.libs"  value="${lib}"/>
  <property name="doc.base"       value="${basedir}/docs" />
  <property name="doc"            value="${doc.base}/api" />
  <property name="resources"      value="${basedir}/resources" />
  <property name="web"            value="${basedir}/webapps/deegree" />
  <property name="bak"            value="${basedir}/bak" />
  <property name="tmp"            value="${basedir}/tmp" />
  <property name="test.project"   value="${basedir}/test" />
  <property name="version.props"  value="${src}/org/deegree/framework/version/version.properties"/>
  <property name="buildId.props"  value="${src}/org/deegree/framework/version/buildId.properties"/>

  
  <!-- todo rename to dist.root --> 
  <property name="deploy.root"    value="${dist}/deploy" />
  <property name="deploy.tomcat"  value="${deploy.root}/tomcat" />
  <property name="deploy.jboss"   value="${deploy.root}/jboss" />
  <property name="deploy.wls"     value="${deploy.root}/wls" />
  <property name="deploy.was"     value="${deploy.root}/was" />

  <!-- all properties are read from the build.properties file -->
  <property file="build.properties"/>

  <!-- version + buildId properties -->
  <property file="${src}/org/deegree/framework/version/version.properties"/>  
  <property file="${src}/org/deegree/framework/version/buildId.properties"/>

  <!-- set build classpath -->
  <path id="build.path">
    <pathelement location="${build}" />
    <fileset dir="${lib}" >
      <include name="**/*.jar" />
      <include name="**/*.zip" />
    </fileset>
    <pathelement location="${java.home}/../lib/tools.jar"/>
    <fileset dir="${optional.libs}" >
      <include name="**/*.jar" />
      <include name="**/*.zip" />
    </fileset>
  </path>

  <!-- set dependency classpath -->
  <path id="dependency.path">
    <fileset dir="${lib}" >
      <include name="**/*.jar" />
      <include name="**/*.zip" />
      <exclude name="${exclude.dependency.jars}"/>  
    </fileset>
  </path>
  
  
  <!-- ant contrib optional task def -->
  <taskdef resource="net/sf/antcontrib/antlib.xml">
    <classpath>
      <pathelement location="${ant.home}/lib/ant-contrib.jar" />
    </classpath>
  </taskdef>
